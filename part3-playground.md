# VMware Private AI Foundation 완벽 가이드 - Part 3
## AI 플레이그라운드 개념 및 역할별 워크플로우 (5-6장)

> **문서 버전:** 3.1 (v3.1 업데이트)  
> **기반 버전:** VCF 9.0.1, PAIF 9.0, PAIS 2.0.89, DLVM 9.0.1

---

## 5. AI 플레이그라운드 개념

### 5.1 정의

**"AI 플레이그라운드"**는 물리적 구성 요소가 아닌 **개념적 영역**입니다.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                    "AI 플레이그라운드" = 개념적 영역                          │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │                                                                     │  │
│   │   • 인프라/플랫폼 팀이 PAIF/PAIS 환경을 구축하여 완성               │  │
│   │                                                                     │  │
│   │   • 개발자들이 자유롭게 AI 에이전트 개발을 반복 시도하는 공간       │  │
│   │                                                                     │  │
│   │   • 개발/QA/스테이징/프로덕션 모두 이 영역 안에서 진행              │  │
│   │                                                                     │  │
│   │   • 물리적 경계가 아닌 논리적/정책적 경계로 환경 분리               │  │
│   │                                                                     │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

> **📌 참고:** "AI 플레이그라운드"와 "PAIS Playground"는 다른 개념입니다.
> 상세 구분은 Part 1의 1.3절을 참조하세요.

### 5.2 환경 분리 = 네임스페이스 분리

AI 플레이그라운드 내에서 개발/QA/스테이징/프로덕션 환경은 **Supervisor Namespace**로 분리됩니다.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    AI 플레이그라운드 (PAIF/PAIS)                             │
│                                                                             │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│   │  DEV-NS     │  │  QA-NS      │  │ STAGING-NS  │  │  PROD-NS    │      │
│   │             │  │             │  │             │  │             │      │
│   │ • DLVM-A    │  │ • Model EP  │  │ • Model EP  │  │ • Model EP  │      │
│   │ • DLVM-B    │  │ • Agent     │  │ • Agent     │  │ • Agent     │      │
│   │ • Model EP  │  │ • KB        │  │ • KB        │  │ • KB        │      │
│   │ • Agent     │  │             │  │             │  │ • VKS Cluster│     │
│   │ • KB        │  │             │  │             │  │ • Apps      │      │
│   │             │  │             │  │             │  │             │      │
│   │ 목적:       │  │ 목적:       │  │ 목적:       │  │ 목적:       │      │
│   │ 실험/개발   │  │ 기능 검증   │  │ 성능 검증   │  │ 실제 서비스 │      │
│   └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘      │
│                                                                             │
│   ┌───────────────────────────────────────────────────────────────────────┐│
│   │                    공유 자원 (전체 네임스페이스 공통)                   ││
│   │                                                                       ││
│   │   • Harbor Registry (모델/이미지 저장소)                              ││
│   │   • DSM + pgvector (벡터 데이터베이스)                                ││
│   │   • VCF Automation (셀프서비스 카탈로그)                              ││
│   │   • OIDC IdP (통합 인증)                                              ││
│   │                                                                       ││
│   └───────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.3 네임스페이스별 리소스 격리

| 리소스 | 네임스페이스별 격리 | 공유 방식 |
|--------|-------------------|----------|
| DLVM | ✅ 격리됨 | 각 NS에서 독립 배포 |
| Model Endpoint | ✅ 격리됨 | 각 NS에서 독립 배포 |
| Agent | ✅ 격리됨 | 각 NS에서 독립 구성 |
| Knowledge Base | ✅ 격리됨 | 각 NS에서 독립 생성 |
| VKS Cluster | ✅ 격리됨 | 각 NS에서 독립 배포 |
| Harbor 모델 | ❌ 공유 | 프로젝트 권한으로 접근 제어 |
| pgvector DB | ❌ 공유 | 스키마/테이블로 격리 |

### 5.4 DLVM만 사용 vs PAIS 활용 비교

| 관점 | DLVM만 (PAIS 없음) | PAIS 활용 |
|------|-------------------|-----------|
| **모델 공유** | 각 DLVM에서 개별 실행 → IP:Port 공유 | 중앙 Model Endpoint → 표준 API URL |
| **추론 엔진** | 각자 설치/관리 (vLLM, Triton) | PAIS가 자동 배포/관리 |
| **GPU 관리** | VM별 고정 할당 | PAIS가 동적 할당 (VKS 기반) |
| **API Gateway** | 없음 (직접 구현) | PAIS 제공 (OpenAI 호환) |
| **RAG 구성** | 직접 LangChain 코딩 | Agent Builder GUI |
| **Knowledge Base** | 직접 벡터 DB 설정 | Data Indexing & Retrieval |
| **프로덕션 전환** | 컨테이너화 + Helm 배포 (큰 작업) | 이미 프로덕션급 (거의 무작업) |
| **대상 사용자** | DevOps 역량 있는 개발자 | 일반 개발자, 데이터 사이언티스트 |
| **유지보수** | 모든 것을 직접 관리 | VMware가 관리형 서비스 제공 |

### 5.5 DLVM 사용 시나리오별 가이드

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        DLVM 사용 여부 결정 가이드                            │
│                                                                             │
│   [질문 1] 모델 선택/평가/튜닝이 필요한가?                                   │
│       │                                                                     │
│       ├── YES ──▶ DLVM 필요 (Data Scientist)                               │
│       │                                                                     │
│       └── NO ───▶ [질문 2] 모델을 Harbor에 Push해야 하는가?                 │
│                       │                                                     │
│                       ├── YES ──▶ DLVM 필요 (MLOps Engineer)               │
│                       │                                                     │
│                       └── NO ───▶ [질문 3] AI API를 호출하는 앱 개발인가?   │
│                                       │                                     │
│                                       ├── YES ──▶ DLVM 불필요              │
│                                       │           (App Developer)           │
│                                       │           로컬 PC에서 개발          │
│                                       │                                     │
│                                       └── NO ───▶ 인프라 관리인가?          │
│                                                       │                     │
│                                                       └── YES ──▶ DLVM 불필요│
│                                                           (Platform Engineer)│
│                                                           vSphere/VCF 도구   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.6 Model Endpoint vs Agent 비교

| 구분 | Model Endpoint | Agent |
|------|---------------|-------|
| **역할** | 단일 모델 API 노출 | RAG 파이프라인 전체 캡슐화 |
| **입력** | 프롬프트 텍스트 | 사용자 질문 |
| **출력** | LLM 응답만 | 응답 + 출처 + 세션 관리 |
| **RAG** | ❌ 없음 | ✅ 내장 |
| **Knowledge Base** | ❌ 연결 불가 | ✅ 연결 가능 |
| **세션 관리** | ❌ 없음 | ✅ 대화 히스토리 유지 |
| **사용 대상** | 직접 RAG 구현 시 | **일반적인 RAG 앱 개발** |
| **API 예시** | `/v1/chat/completions` | `/v1/agents/{name}/chat` |

**권장:** 대부분의 경우 **Agent**를 사용하는 것이 간편합니다.

---

## 6. 역할별 워크플로우

### 6.1 전체 워크플로우 개요

```
┌────────────────────────────────────────────────────────────────────────────────┐
│                          전체 워크플로우 (End-to-End)                          │
│                                                                                │
│  ┌──────────────────────────────────────────────────────────────────────────┐ │
│  │ Phase A: 인프라 구축 (Platform Engineer / VI Admin)                      │ │
│  │                                                                          │ │
│  │  VCF 배포 → GPU WD → Harbor/DSM → PAIS 설치 → 카탈로그 생성             │ │
│  │                                                                          │ │
│  │  [산출물] AI 플레이그라운드 완성                                          │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
│                                      │                                        │
│                                      ▼                                        │
│  ┌──────────────────────────────────────────────────────────────────────────┐ │
│  │ Phase B: 모델 준비 (Data Scientist + MLOps Engineer)                     │ │
│  │                                                                          │ │
│  │  DLVM 배포 → 모델 다운로드 → 검증/튜닝 → Harbor Push → Endpoint 생성    │ │
│  │                                                                          │ │
│  │  [산출물] Model Endpoint URL + 최적 설정 가이드                           │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
│                                      │                                        │
│                                      ▼                                        │
│  ┌──────────────────────────────────────────────────────────────────────────┐ │
│  │ Phase C: RAG 구성 (Data Scientist)                                       │ │
│  │                                                                          │ │
│  │  Data Source 연결 → Knowledge Base 생성 → Agent 생성 → Playground 테스트 │ │
│  │                                                                          │ │
│  │  [산출물] Agent API URL + 프롬프트 가이드                                 │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
│                                      │                                        │
│                                      ▼                                        │
│  ┌──────────────────────────────────────────────────────────────────────────┐ │
│  │ Phase D: 앱 개발 및 배포 (App Developer + DevOps)                        │ │
│  │                                                                          │ │
│  │  API 연동 코드 → Frontend/Backend → 컨테이너 빌드 → VKS 배포            │ │
│  │                                                                          │ │
│  │  [산출물] 프로덕션 AI 애플리케이션                                        │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
│                                                                                │
└────────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 Phase B 상세: 모델 준비 워크플로우

#### 6.2.1 Step 1: DLVM 배포

**VCF Automation 카탈로그에서 배포:**

```
VCF Automation > Catalog > "AI Workstation" 또는 "AI RAG Workstation"
```

**배포 시 선택 옵션:**

| 옵션 | 설명 | 권장값 |
|------|------|--------|
| VM Class | GPU 사양 선택 | vgpu-a100-40c (개발), vgpu-a100-80c (대형 모델) |
| Software Bundle | 사전 설치 프레임워크 | PyTorch (권장), CUDA Samples |
| vCPU / Memory | 컴퓨팅 리소스 | 8 vCPU / 32GB (최소) |
| Storage | 모델 저장 공간 | 200GB+ (대형 모델용) |
| NGC API Key | vGPU 드라이버 다운로드용 | Personal 또는 Service Key |

> **⚠️ TensorFlow 번들은 Deprecated 예정입니다.** PyTorch 사용을 권장합니다.

**배포 완료 후 DLVM에 포함된 것:**

```
DLVM 기본 구성 (DLVM 9.0.1)
├── OS: Ubuntu 22.04 LTS
├── GPU Driver: NVIDIA vGPU Guest Driver (자동 설치)
├── Container Runtime
│   ├── Docker Engine
│   └── NVIDIA Container Toolkit v1.17.1
├── Development Environment
│   ├── JupyterLab (웹 브라우저 접속)
│   ├── Python 3.11
│   └── CUDA Toolkit
├── CLI Tools
│   ├── vcf pais CLI (⚠️ Deprecated 예정)
│   └── Docker CLI
└── Pre-installed Libraries (선택한 번들에 따라)
    ├── PyTorch + torchvision
    ├── Hugging Face Transformers
    └── LangChain
```

**접속 방법:**

```bash
# SSH 접속
ssh vmware@<dlvm_ip>
# 기본 비밀번호: 배포 시 설정한 값

# JupyterLab 접속 (웹 브라우저)
https://<dlvm_ip>:8888
# 토큰: 배포 시 설정한 값 또는 VM 콘솔에서 확인
```

#### 6.2.2 Step 2: 모델 다운로드

```bash
# DLVM에 SSH 접속 후

# 방법 1: Hugging Face에서 다운로드
pip install huggingface_hub
huggingface-cli download meta-llama/Llama-3.1-8B-Instruct \
  --local-dir ./llama-3.1-8b-instruct

# 방법 2: NGC에서 다운로드 (NVIDIA 최적화 모델)
# NGC CLI 설치 필요
ngc registry model download-version nvidia/nemo/megatron_llama3_8b:1.0

# 다운로드 확인
ls -la ./llama-3.1-8b-instruct/
```

#### 6.2.3 Step 3: 모델 검증

```bash
# 1. 파일 무결성 확인
sha256sum ./llama-3.1-8b-instruct/model*.safetensors

# 2. 간단한 추론 테스트 (vLLM 사용)
pip install vllm

# vLLM 서버 시작 (테스트용)
vllm serve ./llama-3.1-8b-instruct \
  --host 0.0.0.0 \
  --port 8000 \
  --tensor-parallel-size 1

# 다른 터미널에서 테스트
curl http://localhost:8000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "./llama-3.1-8b-instruct",
    "messages": [{"role": "user", "content": "Hello, who are you?"}],
    "max_tokens": 100
  }'
```

#### 6.2.4 Step 4: Harbor에 모델 Push

```bash
# 1. Harbor 로그인
docker login harbor.company.com -u <username> -p <password>

# 2. Harbor CA 인증서 신뢰 설정 (필요시)
# macOS
sudo security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain harbor-ca.crt

# Linux
sudo cp harbor-ca.crt /usr/local/share/ca-certificates/
sudo update-ca-certificates

# 3. 모델 디렉토리로 이동 (⚠️ 중요: 반드시 모델 폴더 안에서 실행)
cd ./llama-3.1-8b-instruct

# 4. 모델 Push (⚠️ pais CLI는 Deprecated 예정)
vcf pais models push \
  --modelName meta-llama/llama-3.1-8b-instruct \
  --modelStore harbor.company.com/models \
  -t v1

# 5. Push 확인
# Harbor UI에서 models 프로젝트 > Artifacts 확인
```

**모델 명명 규칙:**

```
harbor.company.com/models/<organization>/<model-name>:<tag>

예시:
harbor.company.com/models/meta-llama/llama-3.1-8b-instruct:v1
harbor.company.com/models/baai/bge-small-en-v1.5:validated
```

#### 6.2.5 Step 5: Model Endpoint 생성

**방법 A: PAIS UI 사용 (권장)**

```
VCF Automation > Build & Deploy > [네임스페이스] > Services > Private AI
> Model Runtime > New Model Endpoint

설정 항목:
┌─────────────────────────────────────────────────────────────────────┐
│                    Model Endpoint 생성 화면                         │
│                                                                     │
│  Endpoint Name: [llama-3-1-8b-instruct]                            │
│                                                                     │
│  Model URL: [harbor.company.com/models/meta-llama/                 │
│              llama-3.1-8b-instruct:v1]                             │
│                                                                     │
│  Model Type: ○ Completion  ○ Embedding                             │
│              ●                                                      │
│                                                                     │
│  Inference Engine: [vLLM] (자동 선택)                               │
│                                                                     │
│  VM Class: [vgpu-a100-40c ▼]                                       │
│                                                                     │
│  Replicas: [1]                                                      │
│                                                                     │
│  [Create]                                                           │
└─────────────────────────────────────────────────────────────────────┘
```

**방법 B: kubectl 사용**

```yaml
# completion-model-endpoint.yaml
apiVersion: pais.vcf.broadcom.com/v1alpha1
kind: ModelEndpoint
metadata:
  name: llama-3-1-8b-instruct
  namespace: dev-ns
spec:
  modelType: COMPLETIONS
  modelEngine: VLLM
  modelUrl: harbor.company.com/models/meta-llama/llama-3.1-8b-instruct:v1
  vmClass: vgpu-a100-40c
  replicas: 1
  registryCredentialsSecret: harbor-registry-creds
```

```bash
kubectl apply -f completion-model-endpoint.yaml

# 배포 상태 확인
kubectl get modelendpoint -n dev-ns
```

**Embedding Model Endpoint (CPU 전용 가능):**

```yaml
# embedding-model-endpoint.yaml
apiVersion: pais.vcf.broadcom.com/v1alpha1
kind: ModelEndpoint
metadata:
  name: bge-small-en
  namespace: dev-ns
spec:
  modelType: EMBEDDINGS
  modelEngine: INFINITY
  modelUrl: harbor.company.com/models/baai/bge-small-en-v1.5:v1
  vmClass: best-effort-small  # GPU 불필요!
  replicas: 1
```

> **💡 비용 절감 팁:** Embedding 모델은 CPU만으로도 충분한 성능을 제공합니다.

### 6.3 Phase C 상세: RAG 구성 워크플로우

#### 6.3.1 Step 1: Data Source 연결

```
VCF Automation > Build & Deploy > [네임스페이스] > Services > Private AI
> Data Indexing & Retrieval > Data Sources > Add Data Source

지원되는 Data Source:
┌─────────────────────────────────────────────────────────────────────┐
│                    Data Source 유형                                 │
│                                                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                │
│  │ Confluence  │  │ SharePoint  │  │ Google      │                │
│  │             │  │             │  │ Drive       │                │
│  │ • Cloud     │  │ • Online    │  │             │                │
│  │ • Server    │  │             │  │ • OAuth 2.0 │                │
│  │ • API Token │  │ • OAuth 2.0 │  │ • Service   │                │
│  │             │  │             │  │   Account   │                │
│  └─────────────┘  └─────────────┘  └─────────────┘                │
│                                                                     │
│  ┌─────────────┐  ┌─────────────┐                                  │
│  │ Amazon S3   │  │ Local       │                                  │
│  │             │  │ Files       │                                  │
│  │ • S3 호환   │  │             │                                  │
│  │ • Access    │  │ • 직접     │                                  │
│  │   Key       │  │   업로드    │                                  │
│  └─────────────┘  └─────────────┘                                  │
└─────────────────────────────────────────────────────────────────────┘
```

**Confluence 연결 예시:**

```
Data Source Name: company-wiki
Type: Confluence
URL: https://company.atlassian.net/wiki
Space Keys: HR, ENGINEERING, PRODUCT  (쉼표로 구분)
Authentication: API Token
Username: admin@company.com
API Token: ••••••••••••••••
```

#### 6.3.2 Step 2: Knowledge Base 생성

```
VCF Automation > Build & Deploy > [네임스페이스] > Services > Private AI
> Data Indexing & Retrieval > Knowledge Bases > Create Knowledge Base

┌─────────────────────────────────────────────────────────────────────┐
│                 Knowledge Base 생성 화면                            │
│                                                                     │
│  Name: [hr-policy-kb]                                              │
│                                                                     │
│  Description: [HR 정책 및 규정 문서 모음]                           │
│                                                                     │
│  Data Sources: ☑ company-wiki (Confluence)                         │
│                ☑ hr-sharepoint (SharePoint)                        │
│                ☐ engineering-docs                                   │
│                                                                     │
│  Embedding Model: [bge-small-en ▼]                                 │
│                   (위에서 생성한 Embedding Endpoint)                │
│                                                                     │
│  Indexing Schedule:                                                 │
│    ○ Manual (수동)                                                  │
│    ● Automatic (자동)                                               │
│       Interval: [Daily ▼]                                          │
│                                                                     │
│  Chunking Settings:                                                 │
│    Chunk Size: [500] tokens                                        │
│    Chunk Overlap: [50] tokens                                      │
│                                                                     │
│  [Create]                                                           │
└─────────────────────────────────────────────────────────────────────┘
```

**PAIS가 자동으로 수행하는 작업:**

```
1. Data Source에서 문서 수집
       ↓
2. 문서 파싱 (PDF, DOCX, HTML 등)
       ↓
3. 텍스트 청킹 (설정된 크기로 분할)
       ↓
4. Embedding 모델로 벡터 변환
       ↓
5. pgvector DB에 저장
       ↓
6. (자동 갱신 설정 시) 주기적으로 1-5 반복
```

#### 6.3.3 Step 3: Agent 생성

```
VCF Automation > Build & Deploy > [네임스페이스] > Services > Private AI
> Agent Builder > Create Agent

┌─────────────────────────────────────────────────────────────────────┐
│                        Agent 구성 화면                              │
│                                                                     │
│  Agent Name: [hr-assistant-agent]                                  │
│                                                                     │
│  Model Endpoint: [llama-3-1-8b-instruct ▼]                         │
│                  (Completion 모델 선택)                             │
│                                                                     │
│  ☑ Add Instructions (System Prompt):                               │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 당신은 회사 HR 정책 전문 어시스턴트입니다.                   │   │
│  │                                                              │   │
│  │ 규칙:                                                        │   │
│  │ 1. 제공된 문서 내용만 기반으로 답변하세요                    │   │
│  │ 2. 확실하지 않으면 "확인이 필요합니다"라고 말하세요          │   │
│  │ 3. 답변은 3문장 이내로 간결하게 하세요                       │   │
│  │ 4. 출처 문서명을 항상 언급하세요                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ☑ Data Indexing & Retrieval:                                      │
│    Knowledge Base: [hr-policy-kb ▼]                                │
│    Similarity Cutoff: [0.7] (0.0 ~ 1.0)                           │
│    Number of Chunks: [5]                                           │
│    Reference Format: ○ Markdown  ● JSON                            │
│                                                                     │
│  Session Parameters:                                                │
│    Chat History Length: [10] messages                              │
│    Session Expiration: [30] minutes                                │
│                                                                     │
│  [Create]                                                           │
└─────────────────────────────────────────────────────────────────────┘
```

#### 6.3.4 Step 4: PAIS Playground 테스트

Agent 생성 후 즉시 **PAIS Playground**에서 테스트할 수 있습니다.

> **📌 용어 구분:** 여기서 "PAIS Playground"는 Agent Builder에 내장된 테스트 UI를 의미합니다. 
> 개념적인 "AI 플레이그라운드"(PAIF/PAIS 전체 개발 공간)와 다릅니다.

```
┌─────────────────────────────────────────────────────────────────────┐
│                        PAIS Playground                              │
│                                                                     │
│  Agent: hr-assistant-agent                                          │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  [User] 연차 휴가는 며칠인가요?                                     │
│                                                                     │
│  [Assistant] 회사 휴가 정책에 따르면, 정규직 직원은 연간 15일의    │
│  연차 휴가가 부여됩니다. 입사 첫 해에는 근무 개월 수에 비례하여    │
│  지급됩니다.                                                        │
│                                                                     │
│  📎 출처: HR_Policy_2026.pdf (12페이지)                            │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│  [메시지 입력...]                                    [Send]         │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│  📋 Sample Code                                                     │
│                                                                     │
│  curl -X POST 'https://pais.company.com/v1/agents/                 │
│    hr-assistant-agent/chat' \                                       │
│    -H 'Authorization: Bearer $TOKEN' \                              │
│    -H 'Content-Type: application/json' \                            │
│    -d '{"message": "연차 휴가는 며칠인가요?",                       │
│         "session_id": "user-123"}'                                  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 6.4 Phase D 상세: 앱 개발 및 배포 워크플로우

#### 6.4.1 App Developer가 받는 정보

MLOps/Data Scientist로부터 전달받는 정보:

```yaml
# API 연동 정보 (예시)
pais_config:
  base_url: "https://pais.company.com"
  agent_name: "hr-assistant-agent"
  
  # 인증 정보
  auth:
    type: "bearer"
    token_endpoint: "https://vcf-automation.company.com/oauth/token"
    client_id: "app-client-id"
    client_secret: "••••••••"  # 또는 별도 전달
    
  # 사용 가이드
  guidelines:
    - "세션 ID는 사용자별로 고유하게 유지하세요"
    - "토큰 만료 시 자동 갱신 로직 구현 필요"
    - "응답의 references 필드에서 출처 정보 추출 가능"
```

#### 6.4.2 App Developer 개발 환경 (로컬 PC)

```
App Developer 작업 환경
├── 로컬 PC (Windows/Mac/Linux)
│   ├── IDE: VS Code, WebStorm 등
│   ├── 런타임: Node.js, Python 등
│   └── 네트워크: 회사 VPN (PAIS 접근용)
│
├── 프로젝트 구조
│   my-ai-app/
│   ├── frontend/           # React/Vue/Next.js
│   │   ├── src/
│   │   │   ├── components/
│   │   │   │   └── ChatWindow.tsx
│   │   │   └── api/
│   │   │       └── paisClient.ts
│   │   └── package.json
│   │
│   ├── backend/            # FastAPI/Node.js
│   │   ├── main.py
│   │   ├── routers/
│   │   │   └── chat.py
│   │   └── requirements.txt
│   │
│   ├── docker-compose.yaml
│   └── Dockerfile
│
└── DLVM 접속: ❌ 불필요
```

#### 6.4.3 API 연동 핵심 정보

**인프라/플랫폼 담당자가 개발팀에 전달해야 할 정보:**

| 항목 | 예시 값 | 설명 |
|------|---------|------|
| Base URL | `https://pais.company.com` | PAIS 서비스 엔드포인트 |
| Agent Name | `hr-assistant-agent` | 생성된 Agent 이름 |
| Auth Type | `OAuth 2.0 Client Credentials` | 인증 방식 |
| Token Endpoint | `https://vcf-auto.company.com/oauth/token` | 토큰 발급 URL |
| Timeout 권장 | `60초 이상` | LLM 응답 대기 (70B 모델은 90초+) |

**Agent API 호출 패턴 (Python):**

```python
# 1. 토큰 획득
token_response = requests.post(TOKEN_ENDPOINT, data={
    "grant_type": "client_credentials",
    "client_id": CLIENT_ID,
    "client_secret": CLIENT_SECRET
})
token = token_response.json()["access_token"]

# 2. Agent API 호출
response = requests.post(
    f"{BASE_URL}/v1/agents/{AGENT_NAME}/chat",
    headers={"Authorization": f"Bearer {token}"},
    json={"message": "연차 휴가는 며칠인가요?", "session_id": "user-123"},
    timeout=60
)

# 3. 응답 구조
# {"response": "답변...", "references": [{"source": "HR_Policy.pdf", "content": "..."}]}
```

**개발팀 가이드라인:**

- 세션 ID는 사용자별로 고유하게 유지 (대화 맥락 유지용)
- 토큰 만료(보통 1시간) 전 자동 갱신 로직 구현 필요
- 응답의 `references` 필드에서 출처 정보 추출 가능
- 에러 시 HTTP 상태 코드와 응답 body의 error 필드 확인

> **📚 참고:** 전체 API 클라이언트 구현 예시는 VMware 공식 샘플 저장소 참조

### 6.5 페르소나별 책임 요약

| Phase | VI Admin | Cloud Admin | Data Scientist | MLOps Engineer | App Developer | DevOps |
|-------|----------|-------------|----------------|----------------|---------------|--------|
| **A. 인프라** | VCF, GPU WD, vGPU | PAIS 설치, 카탈로그 | - | - | - | - |
| **B. 모델 준비** | - | - | 모델 평가/선택 | DLVM 배포, Harbor Push, Endpoint 생성 | - | - |
| **C. RAG 구성** | - | - | KB 생성, Agent 구성, 프롬프트 튜닝 | Embedding Endpoint | - | - |
| **D. 앱 개발** | - | - | - | API 정보 전달 | 앱 코드 개발 | VKS 배포, CI/CD |

---

**[Part 3 완료]**

다음 Part 4에서는 다음 내용을 다룹니다:
- 7장: 개발 환경별 시나리오 (DLVM만 사용, PAIS 활용, 하이브리드)
- 8장: AI 앱 개발 상세 (코드 예시, 프로젝트 구조, 배포 전략)
